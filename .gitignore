# 대용량 데이터 / 모델 (Git LFS 또는 별도 전송)
data/
models/

# 테스트 결과 (실행 시 생성) — 단, 제출용 정본 junit.xml은 추적하여 결과파일.zip에 포함
test-results/*
!test-results/junit.xml

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
venv/
.venv/
.venv-test/
env/
*.egg-info/
dist/
build/

# 환경변수
.env
.env.*
!.env.example

# Claude Code 설정 — 개인 설정(settings.local.json)은 커밋 제외,
# 공유 hook/설정(settings.json + hooks/)은 추적하여 클론에 따라가게 함
.claude/*
!.claude/settings.json
!.claude/hooks/

# macOS
.DS_Store
.AppleDouble

# IDE
.vscode/
.idea/
*.swp
*.swo

# ONNX / 모델 임시 파일
*.onnx
*.bin
*.npy
*.pt
*.pth

# 데이터
.jpg
musinsa_out/
musinsa_out.zip

# 온디바이스 VTON (Mobile-VTON) — 런타임 스크래치 / 가중치 (checkpoint는 gdown 별도 배포)
src/ondevice_vton/_vton_run/
src/ondevice_vton/logs/
src/ondevice_vton/output/
src/ondevice_vton/checkpoint/
